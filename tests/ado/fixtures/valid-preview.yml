# Valid simple pipeline for preview testing
name: Valid Preview Test Pipeline

trigger: none  # Manual trigger only for testing

pool:
  vmImage: ubuntu-latest

steps:
  - script: echo "Hello from preview test!"
    displayName: 'Echo test message'
  
  - script: echo "Preview test completed successfully"
    displayName: 'Completion message'