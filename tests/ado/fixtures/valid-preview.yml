# Pipeline URL: https://dev.azure.com/RussellBoley/ado-mcp/_build?definitionId=74
name: preview-test-valid

trigger: none

jobs:
- job: PreviewJob
  pool: server
  steps:
  - task: Delay@1
    inputs:
      delayForMinutes: '0'